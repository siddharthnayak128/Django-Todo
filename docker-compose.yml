version: "3.9"

services:
  web:
    build: .
    container_name: siddsy_django
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DJANGO_SETTINGS_MODULE=todowoo.settings